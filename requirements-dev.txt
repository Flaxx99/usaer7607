# === Dev / Tooling Dependencies ===
# No son necesarias en producción.
# Instalar: pip install -r requirements-dev.txt

# Linter + formateador unificado
ruff>=0.15.0

# Seguridad estática
bandit>=1.9.0

# Auditoría de vulnerabilidades en dependencias
pip-audit>=2.10.0

# Type checking (opcional — requiere django-stubs para ser útil)
# mypy>=2.0.0
# django-stubs>=6.0.0

# Test runner
pytest>=9.0.0
pytest-django>=4.12.0
pytest-mock>=3.15.0

# Pre-commit
pre-commit>=4.0.0
